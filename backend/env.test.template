# Configuración de Base de Datos para Tests
# Copia este archivo a .env.test y ajusta los valores según tu entorno

# Base de datos de test (recomendado: crear una BD separada para tests)
TEST_DB_HOST=localhost
TEST_DB_PORT=3306
TEST_DB_USER=root
TEST_DB_PASS=tu_password
TEST_DB_NAME=pelu_turnos_test

# Si no defines TEST_DB_*, se usarán las variables DB_* por defecto
# DB_HOST=localhost
# DB_PORT=3306
# DB_USER=root
# DB_PASS=tu_password
# DB_NAME=pelu_turnos

# JWT Secret para tests
JWT_SECRET=test-secret-key-change-in-production

# Otras variables de entorno para tests
NODE_ENV=test

